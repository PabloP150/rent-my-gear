[project]
name = "rent-my-gear-scripts"
version = "0.1.0"
description = "Helper scripts for Rent my Gear"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0.1",
    "google-cloud-storage>=2.18.0",
    "requests>=2.32.0",
]
